<%= form_for(room) do |f| %>
<% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <b>room number</b><br>
    <%= f.text_field :number %>
  </p>

  <p>
    <b>room rate</b><br>
    <%= f.text_field :price %>
  </p>

<p>
    <b>type</b><br>
    <%= f.select :room_type, ["High","Mid" ,"Low"] %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>